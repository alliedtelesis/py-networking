{% if os == 'awp' %}
system: awp_system
{% elif os == 'ats' %}
system: ats_system
{% endif %}

features:
{% if os == 'awp' %}
  vlan: awp_vlan
  interface: awp_interface
  user: awp_user
  file: awp_file
  license: awp_license
  mac: awp_mac
  clock: awp_clock
{% elif os == 'ats' %}
  interface: ats_interface
  vlan: ats_vlan
  user: ats_user
  file: ats_file
  mac: ats_mac
  clock: ats_clock
{% endif %}

